# guava-site — hard rules

- Only touch this repository.
- Never call Linear MCP; only `guava-os pm`.
- Never commit or push unless the operator asks.
- Subagents never merge to staging/production; GitHub authorizes merges.
